using UnityEngine;
using System.Collections;

public class csCam : MonoBehaviour {

	public Transform player;
	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
		//transform.position = player.position + player.transform.forward * (3f);
	}
}
